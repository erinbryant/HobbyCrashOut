# HobbyCrashOut Backend

Simple Express backend for HobbyCrashOut.

Usage

1. Install dependencies:

   npm install

2. Run in development (auto-restart):

   npm run dev

3. Or run production:

   npm start

Endpoints

- GET /api/hello -> { message }
- POST /api/echo -> echoes JSON body
